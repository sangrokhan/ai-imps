model:
  name: "den"
  hidden_dims: [128, 64]

runner:
  name: "den_runner"
  epochs: 20
  lr: 0.001
  l1_lambda: 0.0001
  expansion_k: 10
  drift_threshold: 0.02
  input_dim: 784  # MNIST example
  output_dim: 10
